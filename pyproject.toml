[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transcript-to-ad"
version = "0.1.0"
description = "Convert transcripts into short ad copy and storyboards"
readme = "README.md"
requires-python = ">=3.8"

[tool.pytest]
addopts = ["-q"]
